% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SA24204137R.R
\name{ridge_cv}
\alias{ridge_cv}
\title{Ridge Regression for optimal lambda selection}
\usage{
ridge_cv(X, y, lambdas = seq(0, 10, by = 0.1), k = 5)
}
\arguments{
\item{X}{A numeric matrix or data frame of predictor variables (independent variables).}

\item{y}{A numeric vector of the response variable (dependent variable).}

\item{lambdas}{A numeric vector of lambda values to consider in the model.}

\item{k}{The number of folds for cross-validation. Default is 5.}
}
\value{
The best lambda value that minimizes the Mean Squared Error (MSE) from cross-validation.
}
\description{
Ridge Regression for optimal lambda selection
}
\examples{
\dontrun{
data(mtcars)
# Perform cross-validation for ridge regression on the mtcars dataset
X <- mtcars[, -1]  # Predictor variables (excluding the first column)
y <- mtcars[, 1]   # Response variable (the first column)
best_lambda <- ridge_cv(X, y, lambdas = seq(0, 10, by = 0.1), k = 5)
print(best_lambda)
}
}
